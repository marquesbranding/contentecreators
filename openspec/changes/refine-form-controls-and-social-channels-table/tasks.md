## 1. Compact form controls (base components)

- [x] 1.1 `src/shared/components/ui/input.tsx`: reduce `h-11` to `h-9`, reduce horizontal padding (`px-2.5` → `px-2.5` if already tight, adjust as needed), drop `md:text-sm` split in favor of `text-sm` everywhere for consistent density.
- [x] 1.2 `src/shared/components/ui/select.tsx` (`SelectTrigger`): reduce `h-11` to `h-9`, reduce padding to match `Input`.
- [x] 1.3 `src/shared/components/ui/textarea.tsx`: reduce default min-height/padding to match the new density (check current values first).
- [x] 1.4 Sweep other per-instance `h-11`/`h-12` overrides that were added on top of the base components (e.g. `TextField`/`ControlledSelect` in `profile-form-fields.client.tsx` explicitly set `className="h-12 rounded-xl"`) and remove the redundant override so they inherit the new base size instead of fighting it. Also bumped `input-group.tsx`'s base from `h-8` to `h-9` to match. Remaining `h-11 rounded-xl` overrides inside the social channels table are handled together with the table restructure (group 5).

## 2. Shared searchable `Combobox` component

- [x] 2.1 Create `src/shared/components/ui/combobox.tsx` wrapping `@base-ui/react/combobox`, single-select mode: `Combobox`, `ComboboxInputGroup`/`ComboboxInput`, `ComboboxContent`, `ComboboxItem`, `ComboboxEmpty`, styled to match `select.tsx`'s visual language at the new compact size. (`items` passed to `Combobox` must be rendered via `ComboboxContent`'s function-as-children form — `{(item) => <ComboboxItem value={item}>...}` — Base UI's built-in text filtering only applies to that render pattern, not to statically-mapped children.)
- [x] 2.2 Add a multi-select mode/variant (`multiple` prop) rendering selected values as removable chips (`Combobox.Chips`/`Chip`/`ChipRemove`) above the search input.
- [x] 2.3 The primitive's `items`/`value`/`onValueChange` operate on whichever object shape the caller's `ComboboxItem value={...}` uses (Base UI convention); a higher-level convenience wrapper (task 3.x) adapts this to the plain-string `items: Record<string, string>` / `onValueChange: (value: string) => void` shape used by `ControlledSelect` today, extracting `.value` from the `{value,label}` object Base UI hands back.
- [x] 2.4 Component tests for `combobox.tsx` (`combobox.component.test.tsx`): single-select filter/select, empty-state, multi-select filter/select/chip-remove, keyboard navigation (arrow keys + Enter). All passing.

## 3. Migrate existing dropdowns to `Combobox`

- [x] 3.1 `profile-form-fields.client.tsx`: UF selects (both creator/company shared field and the additional-location UF field) move from `ControlledSelect`/`Select` to a new `ControlledCombobox` (mirrors `ControlledSelect`'s API — `options`, `selectedValue`/`initialValue`, `onValueChange: (value: string) => void` — internally bridging Base UI's `{value,label}`-object-based Combobox API back to plain strings).
- [x] 3.2 `profile-form-fields.client.tsx`: company "Segmento", "Tamanho da empresa", and "Rede social" (7 options, bonus — benefits from search) selects move to `ControlledCombobox`. `creatorType` (2 options) deliberately left on `Select`/`ControlledSelect` per the binary-select exception.
- [x] 3.3 Audited and migrated every remaining `Select` usage to a new shared `src/shared/components/ui/searchable-select.tsx` (`SearchableSelect`, a `Combobox`-based near-drop-in for `Select`'s own `items`/`value`/`onValueChange` Record API): `moderation-queue-view.client.tsx` (status, order), `account-management-view.client.tsx` (role, status, archive, order), `audit-history-view.client.tsx` (actor type, action, source), `admin-email-outbox-view.client.tsx` (status, template, order), `catalog-filter-controls.client.tsx` (niche, platform, city, state), `sponsorship-management-view.client.tsx` (placement type, audience — both the create/edit form and the filter bar — plus status filter), `company-carousel-screen.client.tsx` (segment). Left on `Select` (binary/near-binary, no search value): `creatorType` (2 options), catalog `Tipo de criador` (3), `backoffice-analytics-dashboard` período (3). Reordered each migrated `items` Record literal to match the previous curated `SelectItem` display order where those two orders had diverged in the original code (the old `Select` let `items` and the rendered list order differ; `SearchableSelect` derives display order from `items` directly).

## 4. Niche picker: checklist → searchable multi-select

- [x] 4.1 `profile-form-fields.client.tsx`: replace the "Principais nichos" checkbox list with the multi-select `Combobox` (chips + search), sourced from the same `creatorNicheOptions` data (the "Envie sua sugestão" custom-text flow is unchanged once that option is selected). Selection is submitted as one hidden `<input type="hidden" name="nicheSlugs">` per selected slug — same `formData.getAll("nicheSlugs")` contract the server actions already expect — rather than relying on Base UI's own multi-select form-submission format.
- [x] 4.2 Preserved the existing 1–5 selection limit (now enforced by disabling not-yet-selected `ComboboxItem`s once 5 are selected) and its validation/error messaging (`resolveFieldErrors("nicheSlugs")`, via `data-field-value` on `ComboboxInputGroup` read by the existing required-field-validation system).
- [x] 4.3 Updated `profile-form-fields.component.test.tsx` and `combined-registration-form.component.test.tsx` (niche checkbox assertions/interactions → chip-visibility assertions and combobox open+click-option interactions).

## 5. Social channels table restructure

- [x] 5.1 `src/features/onboarding/domain/social-channels-form-data.ts`: remove `"OTHER"` from `SOCIAL_CHANNEL_PLATFORMS`; remove `label` from `SocialChannelFormValue` and its parsing in `readSocialChannels()`.
- [x] 5.2 `src/features/onboarding/schemas/onboarding-form-schema.ts` and `onboarding-draft-schema.ts`: removed `label`/OTHER-specific handling from `socialChannelEntry`/`socialChannelDraftSchema`, and narrowed `platform` on both to a new `creatorSocialChannelPlatform` enum derived from `SOCIAL_CHANNEL_PLATFORMS` (kept the wider `socialPlatform` enum used for company social untouched).
- [x] 5.3 `src/features/onboarding/components/form-error-summary.tsx`: dropped the `label` entry from `socialChannelFieldLabels`.
- [x] 5.4 Repository/action write paths: dropped `handle: channel.label` from `drizzle-onboarding-registration.repository.ts` and `drizzle-influencer-profile.repository.ts` inserts/updates, and the `label` field from `loadProfile`'s returned `socialChannels`; fixed `use-onboarding-autosave.ts`'s draft mapper. **Also found and fixed a real data-loss bug this narrowing exposed**: `loadProfile`/`updateSocialAndMetric` previously operated on *every* row in `social_profiles` regardless of platform, so a creator's pre-existing "Outra" channel (no longer representable in the picker) would silently get archived on their very next profile edit. Added an `isManageableSocialChannel` guard so `loadProfile` only surfaces picker-representable channels in the edit form, and `updateSocialAndMetric`'s archive/primary-reconciliation logic only ever touches those — legacy non-picker channels (e.g. `OTHER`, `TIKTOK`) are left completely untouched, keeping them exactly as stored per the proposal's stated guarantee.
- [x] 5.5 `src/shared/components/social-platform-icon.tsx`: brand-color rendering via `style={{ color: '#' + hex }}` per Simple Icons entry; LinkedIn glyph now uses `#0A66C2`.
- [x] 5.6 `profile-form-fields.client.tsx` social channels table: Principal (star) column is first and narrow (`2.25rem` grid track), always laid out as a persistent grid/table (`grid-cols-[2.25rem_8rem_6rem_1fr]` on both header and rows, no `sm:` breakpoint switching or responsive stacking — corrected after user feedback that the table format must be preserved at all widths, with the earlier `sm:order-*`/`sm:hidden` mobile-stacking approach reverted); every column header left-aligns with its content except Seguidores, which centers (header and input both); removed the per-row `border`/`bg-card` card styling entirely (rows are now unbordered, separated by padding only); table wrapped in `mx-auto w-full max-w-2xl overflow-x-auto` (inner content `min-w-[26rem]`) to center it on the page while still allowing horizontal scroll on narrow phones instead of collapsing the layout.
- [x] 5.7 Removed the "Outra" row, its custom-name input, and the `otherChannelLabel` state entirely.
- [x] 5.8 No test updates were needed for the table restructure/Outra removal — the existing suite had no assertions on the old column order or the "Outra" option, and the full suite (1101 tests) passes unchanged.
- [x] 5.9 Fixed a chip-overflow bug surfaced by the user: `ComboboxInputGroup` (`src/shared/components/ui/combobox.tsx`) used a fixed `h-9`, so a multi-select combobox with wrapped chips (e.g. 5 selected niches wrapping to 3 rows) kept the box at 36px and visually bled into the content below it. Changed to `min-h-9` so the box grows with wrapped content. Verified live: 5 selected niches → box grows to 90px/3 rows, no overlap with the "Localização" section below.
- [x] 5.10 Replaced the grid/table format with a compact per-platform block layout (checkbox+icon+name row, inline Seguidores/Principal row, inline Link do perfil row) after a screenshot appeared to show the grid columns collapsing to a single stacked column.
- [x] 5.11 User clarified the stacked-column screenshot (5.10) was a stale browser tab that hadn't reloaded — the grid/table format was actually rendering correctly, and is the preferred layout after all. Reverted 5.10 back to the grid/table format (header row + per-row `grid grid-cols-[...]`, `overflow-x-auto` wrapper), this time widening the Principal column from `2.25rem` to `3.5rem` (56px) to fix a genuine bug the screenshot also showed: the "PRINCIPAL" header text (uppercase, ~9 characters) overflowed past the old 36px-wide column 1 track and visually overlapped "REDE SOCIAL" text in the next column. Verified live: header spans no longer overlap (Principal ends at x=356.5, Rede social starts at x=368.5, 12px gap), rows align exactly under headers (both start at x=300.5/368.5).

## 6. Location field alignment

- [x] 6.1 `profile-form-fields.client.tsx`: added `items-end` to the Cidade/UF `FieldGroup` grid row so both inputs bottom-align regardless of the description-text height difference above Cidade. The additional-location Cidade/UF fields sit in a general flowing multi-field grid (not a dedicated Cidade+UF pair), so the same issue doesn't apply there — left unchanged.

## 7. Mobile-first audit

- [ ] 7.1 Sign-up / login screens: verify no horizontal overflow, tap targets ≥36px, spacing at 320–390px widths.
- [ ] 7.2 Catalog list + detail pages: verify card grid, filters, and detail metric cards at mobile widths.
- [ ] 7.3 Profile edit (influencer + company): verify the same, including the now-restructured social channels table and niche combobox.
- [ ] 7.4 Backoffice/admin account list, detail, and edit screens: verify no horizontal overflow and usable controls at mobile widths (lower priority if backoffice is desktop-only by design — confirm with existing patterns before spending effort here).

## 8. Tests

- [ ] 8.1 Update/add unit tests for `social-platform-icon.tsx` (brand color rendering).
- [ ] 8.2 Update onboarding schema/action unit tests for the removed `label`/OTHER platform.
- [ ] 8.3 Update onboarding component tests for the new combobox-based niche picker and UF/segmento/tamanho selects.
- [ ] 8.4 Full `npm run type-check` and `npx vitest run --project unit --project component` pass.

## 9. Live verification

- [x] 9.1 Verified via DOM/computed-style inspection (screenshots unreliable in this session's browser pane): social channels table is a persistent `display:grid` at all widths, Principal star column first and widened just enough (`3.5rem`) to fit its header label without overlapping "Rede social", no per-row borders, table centered (`mx-auto max-w-2xl overflow-x-auto`), Seguidores centered, headers aligned exactly with row content (both start at the same x-coordinates), "Outra" gone (7 platforms rendered). See 5.10/5.11 for the back-and-forth that led here.
- [x] 9.2 Verified niche multi-select live: search-filter opens 22 options, selecting up to 5 works (5th selection reached, further options disabled per the limit), chip removal via `ComboboxChipRemove` exercised in component tests, "Envie sua sugestão" custom text flow unchanged (untouched by this fix). Also verified the chip-wrap fix (task 5.9): box grows with wrapped chips instead of overflowing into content below.
- [x] 9.3 Verified UF combobox live on the sign-up creator form (search-filter/selection already covered by `combobox.component.test.tsx`'s keyboard-nav test); segmento/tamanho da empresa are company-only fields, covered by existing component tests.
- [x] 9.4 Verified Cidade/UF alignment fix live via DOM measurement: Cidade input bottom edge and UF combobox box bottom edge both at `1287.46px` — exact match.
- [ ] 9.5 Verify compact control sizing across sign-up, login, catalog, and profile edit at both desktop and mobile widths.
